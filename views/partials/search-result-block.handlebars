{{!-- This partial block returns the listed items from the search to the index.handlebars --}}
{{!-- Like (artist (default), song, key, temp0(? not sure if we want to have this as a searchable parameter) ) --}}